const inviteUser = require('./invite-user')
const onAdminStatusGiven = require('./on-admin-status-given')
const onAdminStatusRevoked = require('./on-admin-status-revoked')
const getAccesses = require('./get-accesses')
const onUserInvited = require('./on-user-invited')
const onUserRemoved = require('./on-user-removed')
const read = require('./read')

function init (deps) {
  return {
    read: read(deps),
    inviteUser: inviteUser(deps),
    getAccesses: getAccesses(deps),
    onAdminStatusGiven: onAdminStatusGiven(deps),
    onAdminStatusRevoked: onAdminStatusRevoked(deps),
    onUserInvited: onUserInvited(deps),
    onUserRemoved: onUserRemoved(deps)
  }
}

module.exports = init
