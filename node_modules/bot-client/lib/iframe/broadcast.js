function init ({ command }) {
  function broadcast (teamId, data = {}, requestId) {
    return command(teamId, 'iframe::send-message', { data: { ...data, requestId } })
  }
  return broadcast
}

module.exports = init
