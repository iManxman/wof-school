function init ({ onBroadcast, loginInfo }) {
  function onSent (cb) {
    if (typeof cb !== 'function') {
      cb = () => {}
      console.log('WARN', 'must provide function to onSent ')
    }
    onBroadcast('mail::sent', cb)
  }
  return onSent
}

module.exports = init
