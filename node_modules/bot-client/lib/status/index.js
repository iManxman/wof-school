const create = require('./create')
const read = require('./read')
const setName = require('./set-name')
function init (deps) {
  return {
    create: create(deps),
    read: read(deps),
    setName: setName(deps)
  }
}

module.exports = init
