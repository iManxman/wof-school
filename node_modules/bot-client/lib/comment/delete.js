function init ({ command }) {
  async function commentDelete (teamId, query = '') {
    const id = query.id || query
    const response = command(teamId, 'comment::delete', { id })
    return response
  }

  return commentDelete
}

module.exports = init
