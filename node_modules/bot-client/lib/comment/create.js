function init ({ command }) {
  async function create (teamId, query = {}) {
    const response = command(teamId, 'comment::create', query)
    return response
  }

  return create
}

module.exports = init
