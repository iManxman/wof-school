const create = require('./create')
const read = require('./read')
const setTitle = require('./set-title')
const setDeadline = require('./set-deadline')
const setStream = require('./set-stream')
const setStatus = require('./set-status')
const setPriority = require('./set-priority')
const setBudget = require('./set-budget')
const setResponsible = require('./set-responsible')
const onCreated = require('./on-created')
const onStatusUpdated = require('./on-status-updated')
const setDescription = require('./set-description')

function init (deps) {
  return {
    create: create(deps),
    read: read(deps),
    setTitle: setTitle(deps),
    setDeadline: setDeadline(deps),
    setResponsible: setResponsible(deps),
    setStream: setStream(deps),
    setStatus: setStatus(deps),
    setPriority: setPriority(deps),
    setBudget: setBudget(deps),
    setDescription: setDescription(deps),
    onCreated: onCreated(deps),
    onStatusUpdated: onStatusUpdated(deps)
  }
}

module.exports = init
