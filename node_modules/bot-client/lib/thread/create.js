function init ({ command }) {
  async function create (teamId, query = {}) {
    const response = await command(teamId, 'thread::create', query)
    return response
  }
  return create
}
module.exports = init
