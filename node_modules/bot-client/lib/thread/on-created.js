function init ({ onBroadcast, loginInfo }) {
  function onCreated (cb) {
    if (typeof cb !== 'function') {
      cb = () => {}
      console.log('WARN', 'must provide function to thread.onCreated')
    }
    onBroadcast('thread::created', cb)
  }
  return onCreated
}

module.exports = init
