function init ({ command }) {
  function _delete (teamId, query = '') {
    const id = query.id || query
    return command(teamId, 'stream::delete', { id })
  }
  return _delete
}
module.exports = init
