const create = require('./create')
const _delete = require('./delete')
const read = require('./read')
const setName = require('./set-name')
const setUser = require('./set-user')
const deleteUser = require('./delete-user')
const onUserSet = require('./on-user-set')
const onUserDeleted = require('./on-user-deleted')
const setAdmin = require('./set-admin')

function init (deps) {
  return {
    create: create(deps),
    delete: _delete(deps),
    deleteUser: deleteUser(deps),
    setName: setName(deps),
    setUser: setUser(deps),
    read: read(deps),
    onUserDeleted: onUserDeleted(deps),
    onUserSet: onUserSet(deps),
    setAdmin: setAdmin(deps)
  }
}

module.exports = init
