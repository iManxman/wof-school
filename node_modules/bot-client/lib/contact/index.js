const read = require('./read')
const create = require('./create')
const getLocale = require('./get-locale')

function init (deps) {
  return {
    read: read(deps),
    create: create(deps),
    getLocale: getLocale(deps)
  }
}
module.exports = init
